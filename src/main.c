#include "stack.h"
#include "renderer.h"

int main(void) {

    if (!initSDL()){
        return 1;
    }
    /*Stack *s = newStack();

    stackPush(s, 24);
    stackPush(s, 24);
    stackPush(s, 24);

    //stackPop(s);

    stackShow(s);
    free(s);*/
    
}
